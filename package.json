{
  "name": "baufest",
  "version": "1.0.0",
  "description": "Programming Exercise",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:report": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/combined-report.json && npx marge cypress/reports/combined-report.json --reportDir cypress/reports --inline"
  },
  "author": "Jose Raul Alzibar",
  "license": "ISC",
  "devDependencies": {
    "ajv": "^8.12.0",
    "cypress": "^13.6.1",
    "mochawesome": "^7.1.3"
  },
  "dependencies": {
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false
  }
}
